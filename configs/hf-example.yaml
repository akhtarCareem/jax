models:
  - name: feature_encoder
    source: hf
    hf_repo: careem/jax-mock-mlp
    params_path: params
    params_format: orbax_standard
    artifact_name: feature_encoder
    default_platform: cpu
    max_batch_size: 8
    warmup_requests:
      - features: [[0.1, 0.2, 0.3, 0.4]]

cache_dir: .cache/jax-server
